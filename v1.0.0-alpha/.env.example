# Elite Dangerous NEXT - Environment Variables / Переменные окружения
# Copy this file to .env and fill in your values / Скопируйте этот файл в .env и заполните значения

# =============================================================================
# Server Configuration / Конфигурация сервера
# =============================================================================

# Port for the HTTP server (default: 3000) / Порт для HTTP сервера (по умолчанию: 3000)
PORT=3000

# Node environment: development, production, test / Окружение Node.js: development, production, test
NODE_ENV=development

# =============================================================================
# CORS Configuration / Конфигурация CORS
# =============================================================================

# Comma-separated list of allowed origins for CORS (production only) / Список разрешённых источников через запятую (только для production)
# Example / Пример: CORS_ORIGINS=https://example.com,https://app.example.com
CORS_ORIGINS=

# CORS origin (default: allow all in development)
# CORS_ORIGIN=http://localhost:3000

# =============================================================================
# Elite Dangerous Journal Configuration / Конфигурация журнала Elite Dangerous
# =============================================================================

# Path to Elite Dangerous journal directory / Путь к директории журналов Elite Dangerous
# Windows default / Windows по умолчанию: %USERPROFILE%\Saved Games\Frontier Developments\Elite Dangerous
# Leave empty to use default path / Оставьте пустым для использования пути по умолчанию
JOURNAL_PATH=

# Maximum number of journal files to load on startup (default: 5)
# Установите больше для загрузки большей истории, меньше для быстрого запуска
# Set higher to load more history, lower for faster startup
MAX_HISTORICAL_FILES=5

# =============================================================================
# Database Configuration / Конфигурация базы данных
# =============================================================================

# Path to SQLite database file (default: ./data/edcopilot.db) / Путь к файлу SQLite базы данных (по умолчанию: ./data/edcopilot.db)
# Leave empty to use default path / Оставьте пустым для использования пути по умолчанию
DB_PATH=

# =============================================================================
# Logging Configuration / Конфигурация логирования
# =============================================================================

# Log level: error, warn, info, debug (default: info) / Уровень логирования: error, warn, info, debug (по умолчанию: info)
LOG_LEVEL=info

# =============================================================================
# External API Configuration (Future) / Конфигурация внешних API (Будущее)
# =============================================================================

# EDDN (Elite Dangerous Data Network) - for market and station data / EDDN - для данных рынка и станций
# EDDN_GATEWAY=

# Spansh API - for navigation and routing / Spansh API - для навигации и маршрутизации
# SPANSH_API_URL=

# EDSM (Elite Dangerous Star Map) - for galaxy data / EDSM - для данных о галактике
# EDSM_API_KEY=

# INARA - for commander and faction data / INARA - для данных о командире и фракциях
# INARA_API_KEY=

# =============================================================================
# Frontier OAuth2 Configuration / Конфигурация Frontier OAuth2
# =============================================================================

# Frontier API Client ID (get from https://auth.frontierstore.net/client/signup)
# Or use default EDMC client ID for testing
# FRONTIER_CLIENT_ID=fb88d428-9110-475f-a3d2-dc151c2b9c7a

# Frontier API Client Secret (only needed for some flows)
# FRONTIER_CLIENT_SECRET=

# Base URL for OAuth redirect (usually your app's URL)
# FRONTIER_REDIRECT_BASE_URL=http://localhost:3000
